global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'orders-microservices'

scrape_configs:
  # Scrape Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Scrape Spring Boot services running on Windows localhost
  # host.docker.internal = special DNS name that resolves to Windows host IP
  - job_name: 'spring-boot-local'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets:
        - 'host.docker.internal:8081'
        - 'host.docker.internal:8082'
        - 'host.docker.internal:8083'
        labels:
          environment: 'local'

  # Scrape Spring Boot services running in Docker (keep for production)
  - job_name: 'spring-boot-docker'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets:
        - 'produk-service:8081'
        - 'pelanggan-service:8082'
        - 'order-service:8083'
        labels:
          environment: 'docker'